# flake8: noqa
# fmt:off

# replace with application configuration
# should be a standard python dict object with basic types
# will be parsed and written out with toml writer
DEFAULT_CONFIG = {
    "env": {
        "default": {
            "foo": "bar"
        }
    }
}
